# Variables para Docker Compose - Producción
# Este archivo contiene solo variables usadas por Docker Compose
# Las variables de Laravel están en .env.example en la raíz

# Variable para seleccionar este archivo (usado por docker-compose.yml)
APP_ENV=prod

# Variables para servicio PostgreSQL (Docker Compose)
# En producción, estos valores pueden apuntar a servicios gestionados (RDS, Supabase)
DB_DATABASE=TU_DB
DB_USERNAME=TU_USER
DB_PASSWORD=TU_PASS

# Variables de conexión a servicios Docker o servicios gestionados
# En producción con Docker: usar nombres de servicios (postgres, redis)
# En producción sin Docker: usar hosts reales (rds.amazonaws.com, etc.)
DB_HOST=postgres
DB_PORT=5432
REDIS_HOST=redis
REDIS_PORT=6379
MEILISEARCH_HOST=http://meilisearch:7700

# Nota: Los logs se almacenan en la misma BD con particionamiento mensual
# No se usa BD separada para logs según arquitectura del proyecto
